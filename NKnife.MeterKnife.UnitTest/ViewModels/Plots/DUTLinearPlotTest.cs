using System;
using System.Collections.Generic;
using System.Text;
using FluentAssertions;
using NKnife.MeterKnife.ViewModels.Plots;
using Xunit;

namespace NKnife.MeterKnife.UnitTest.ViewModels.Plots
{
    public class DUTLinearPlotTest
    {
        [Fact]
        public void UpdateRangeTest1()
        {
            // var isFirst = true;
            // var max = 16D;
            // var min = 8D;
            // var pair = DUTLinearPlot.UpdateRange(12, ref isFirst, ref max, ref min);
            // pair.Item1.Should().BeGreaterOrEqualTo(max);
            // pair.Item2.Should().BeLessOrEqualTo(min);
        }
    }
}
